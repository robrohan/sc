---

kanban-plugin: board

---

<!--
## Project Context

**Project:** `sc` — a classic Unix spreadsheet calculator (C99), located at `/Users/robrohan/Projects/SideProjects/sc/`
**Active branch:** `harden`
**Main source:** `src/` — all C files are here; it is busy so tests live outside it

### Build
```
cd src && make        # builds sc binary and psc (printer variant)
cd src && make clean  # removes generated files: pxmalloc.c pvmtbl.c gram.c qhelp.c etc.
```
- Compiler: gcc with C99 + strict warnings: `-Wall -Wextra -Wshadow -Wconversion -Wsign-conversion -Wundef` etc.
- `pxmalloc.c` and `pvmtbl.c` are **generated by make** (hardlinks to xmalloc.c / vmtbl.c) — not in git
- `gram.c` is generated from `gram.y` by bison
- PSC build variant uses `-DPSC` (printer sc, no curses)

### What has been done
- All 81+ compiler warnings eliminated (commit `4c09ede`)
- clang-format applied to all `.c` files using `/.clang-format` in project root
- `sc.h` patched: added `#include <stdio.h>` unconditionally; curses fallback macros
  (`A_CHARTEXT`, `color_set`, `attr_get`) wrapped in `#ifndef PSC` to prevent
  conflicts when clang-format reorders includes putting `<curses.h>` after `"sc.h"`

### Key gotchas
- `is_cleared` is defined as a cell-flag bitmask (`0200`) in `sc.h` — conflicts with
  the curses `is_cleared()` function if `<curses.h>` is included after `"sc.h"`.
  The user fixed this manually before clang-format was re-run.
- Signal handlers `doquit`, `nopipe`, `winchg`, `dump_me` must NOT be static —
  `interp.c` calls `doquit()` directly across translation units.
- clang-format sorts includes alphabetically, moving `"sc.h"` before system headers —
  this can reintroduce macro conflicts; be careful when re-running clang-format on headers.

### Testing goals
- Tests go in a new `tests/` directory at the project root (NOT inside `src/`)
- Tests should have their own `Makefile` and build independently
- Framework not yet chosen — candidates: **Criterion** (modern, self-contained),
  **Unity** (single-file, no install), **Check** (classic)
- Start with `xmalloc.c` (no curses/global deps), then `format.c`, `abbrev.c`, `range.c`
- `interp.c` and `lex.c` are hard — leave for integration tests last

### Module testability summary
| Module     | Testability | Key functions to test |
|------------|-------------|-----------------------|
| xmalloc.c  | Excellent   | scxmalloc, scxrealloc, scxfree (guard magic number) |
| format.c   | Good        | format(), engformat() |
| abbrev.c   | Good        | find_abbr(), add_abbr(), del_abbr() |
| range.c    | Good        | find_range(), add_range(), fix_ranges() |
| sort.c     | Moderate    | compare() comparison function |
| frame.c    | Good        | find_frange(), boundary checks |
| color.c    | Moderate    | find_crange() (curses deps elsewhere) |
| vmtbl.c    | Moderate    | checkbounds() |
| interp.c   | Difficult   | eval() — heavy global state, do last |
| crypt.c    | Very hard   | fork/exec/pipes — integration only |
| lex.c      | Difficult   | generated code, skip for now |
-->


## 🧊 Backlog

- [ ] Write tests for xmalloc.c — scxmalloc, scxrealloc, scxfree guard validation
- [ ] Write tests for abbrev.c — add_abbr, find_abbr, del_abbr linked list operations
- [ ] Write tests for format.c — format() number formatting with various format strings
- [ ] Write tests for format.c — engformat() engineering/scientific notation formatting
- [ ] Write tests for range.c — add_range, find_range, del_range linked list operations
- [ ] Write tests for range.c — fix_ranges row/col adjustment after insert/delete
- [ ] Write tests for sort.c — compare() function with numeric and string cell values
- [ ] Write tests for color.c — find_crange() lookup logic
- [ ] Write tests for frame.c — find_frange() lookup and boundary checking
- [ ] Write tests for vmtbl.c — checkbounds() coordinate clamping
- [ ] Write integration tests for interp.c — eval() with simple arithmetic expression trees
- [ ] Write integration tests for interp.c — aggregation functions (dosum, doavg, docount) with fixture table
- [ ] Add a `make test` target to src/Makefile that builds and runs the test suite
- [ ] Set up CI to run tests on each push

## 📝 Todo

- [ ] Detect .csv extension in readfile() in src/cmds.c and call parse_csv() directly instead of yyparse()
- [ ] Implement parse_csv(): read fields, detect numeric vs string, call label()/let() per cell in-process
- [ ] Handle RFC 4180 edge cases in parse_csv(): quoted fields, embedded commas, escaped quotes
- [ ] Test CSV loading with a sample file covering strings, numbers, quoted fields, and empty cells
- [ ] Update README.md to document CSV loading support

## 👨‍💻 Doing (0)

## ✅ Done

**Complete**
- [x] Refactor psc.c: move field-parsing logic into new src/csv.c / src/csv.h, leave psc.c as a thin main() that calls into csv.c #claude
- [x] Choose a C unit test framework (Check, Criterion, or Unity) and add it to the build
- [x] Create tests/ directory with a Makefile that builds and runs tests independently of src/
- [x] Write tests for csv.c — 16 tests covering numerics, strings, separators, quoted fields, empty fields, multi-line, alternate delimiters
